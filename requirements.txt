# =============================================================================
# Python-зависимости для проекта hackathon_robot (ROS 2)
# =============================================================================
# Установка одной командой:
#   pip install -r requirements.txt
#
# Рекомендуется использовать тот же интерпретатор Python, с которым собран ROS 2
# для вашего дистрибутива (например /opt/ros/humble/lib/python3.10/site-packages),
# чтобы версии cv_bridge и numpy не расходились с системой.
#
# Системное ПО (не ставится через pip): ROS 2, espeak-ng, модели Vosk — см. README.md
# =============================================================================

# --- Базовые (дублируют install_requires пакета hackathon_robot) ---
setuptools>=65.0
PyYAML>=6.0
numpy>=1.23

# --- Зрение: HOG, yolo_finetune_node, работа с изображениями ---
opencv-python-headless>=4.8.0

# --- Голос офлайн: ASR Vosk + захват микрофона (sounddevice) ---
vosk>=0.3.45
sounddevice>=0.4.6

# --- Дообучение YOLO офлайн (обычно на ноутбуке, не на роботе) ---
ultralytics>=8.0.0

# --- По желанию: тесты ---
# pytest>=7.0
